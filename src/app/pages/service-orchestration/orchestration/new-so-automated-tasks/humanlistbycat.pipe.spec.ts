import { HumanlistbycatPipe } from './humanlistbycat.pipe';

describe('HumanlistbycatPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanlistbycatPipe();
    expect(pipe).toBeTruthy();
  });
});
