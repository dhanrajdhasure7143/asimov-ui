import { CheckResourcePipe } from './check-resource.pipe';

describe('CheckResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
