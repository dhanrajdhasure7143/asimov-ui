import { BotlistbycatPipe } from './botlistbycat.pipe';

describe('BotlistbycatPipe', () => {
  it('create an instance', () => {
    const pipe = new BotlistbycatPipe();
    expect(pipe).toBeTruthy();
  });
});
