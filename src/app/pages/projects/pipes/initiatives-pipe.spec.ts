import {  IntitiativePipe } from "./initiatives-pipe.pipe";

describe('IntitiativePipe', () => {
  it('create an instance', () => {
    const pipe = new IntitiativePipe();
    expect(pipe).toBeTruthy();
  });
});