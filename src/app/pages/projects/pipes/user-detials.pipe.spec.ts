import { UserDetialsPipe } from './user-detials.pipe';

describe('UserDetialsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserDetialsPipe();
    expect(pipe).toBeTruthy();
  });
});
